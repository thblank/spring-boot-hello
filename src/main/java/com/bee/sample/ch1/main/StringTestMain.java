package com.bee.sample.ch1.main;

import org.springframework.util.StringUtils;

import java.util.*;
import java.util.stream.Collectors;

public class StringTestMain {

    public static void main(String[] args) {
//        List<String> list = Arrays.asList("Hollis","hollischuang","Java干货");
//
//        String s = list.stream().collect(Collectors.joining(",","[","]"));
//
//        System.out.println(s);
//        List<Integer> integers = Arrays.asList(1, 2, 30);
        Double i = 4.699999809265137;
        String format = String.format("%.1f", i);
        System.out.println(format);
        /*List<Integer> integers1 = integers.subList(0, 2);

        System.out.println(integers1);*/

        String abd = "abd";
        int num= 20;
        StringTestMain stringTestMain = new StringTestMain();

        if (stringTestMain == null) {
            if (stringTestMain != null) {
                if (stringTestMain != null) {
                    
                }
            }
        }

        List<Integer> list = new ArrayList<>();
        for (Integer integer : list) {
            
        }
        boolean flag = true;
        if (!flag) {

        }


    }
}
